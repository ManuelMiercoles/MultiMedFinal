Run mysql, mariadb, etc using XAMPP,WAMPP, or engine of your choice. 
database.sql and config.php automatically generate a database if none exist, 
provided mysql or other db is running.

Database created in config.php
Tables to fill db generated in database.sql
Open index.html first. 

Modify function has not been implemented, once one has logged-in
as admin, they may enter new users or delete existing entries.
